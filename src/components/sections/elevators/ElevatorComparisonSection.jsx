import { elevatorTypes } from './elevatorsData'

export default function ElevatorComparisonSection() {
  return <section className="px-[clamp(20px,5vw,76px)] py-[clamp(85px,9vw,140px)]"><header className="mx-auto flex max-w-[1440px] items-end justify-between gap-10 max-lg:flex-col max-lg:items-start"><div><p className="m-0 text-[11px] font-semibold uppercase tracking-[.2em] text-[#0F2B45]">Choose with confidence</p><h2 className="mb-0 mt-4 text-[clamp(50px,5vw,78px)] font-medium leading-[.98] tracking-[-.045em]">Understand the difference.</h2></div><p className="m-0 max-w-[520px] text-[16px] leading-[1.7] text-[#0F2B45]">Every Bengaluru project is unique, but these starting points make it easier to identify the right conversation.</p></header><div className="mx-auto mt-14 grid max-w-[1440px] overflow-hidden rounded-[28px] border border-[#F5F3EC] md:grid-cols-2 xl:grid-cols-4">{elevatorTypes.map((item, index) => { const Icon = item.icon; return <article key={item.name} className={`p-[clamp(28px,4vw,48px)] ${index < 3 ? 'border-b border-[#F5F3EC] xl:border-b-0 xl:border-r' : ''} ${index === 1 ? 'md:border-l xl:border-l-0' : ''}`}><Icon className="size-7 text-[#0F2B45]" /><h3 className="mb-0 mt-8 text-[25px] font-medium">{item.name}</h3><div className="mt-8 grid gap-6">{item.details.map(([label, value]) => <div key={label}><span className="text-[10px] font-semibold uppercase tracking-[.14em] text-[#0F2B45]">{label}</span><p className="mb-0 mt-1.5 text-[14px] leading-[1.5] text-[#0F2B45]">{value}</p></div>)}</div></article> })}</div></section>
}
